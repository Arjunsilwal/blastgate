id: harvest-host-credential-files
title: Payload reads credential files from the host filesystem
chain_link: 4
ecosystem: npm
source: constructed
expect: denied
requires: sandbox
note: >
  Link 4, the other half of the harvest. The environment filter covers variables;
  this covers ~/.npmrc, ~/.aws, and SSH keys, and it depends entirely on the
  mount boundary. Not runnable until the sandbox exists, and counted as not-yet-
  runnable rather than skipped.
